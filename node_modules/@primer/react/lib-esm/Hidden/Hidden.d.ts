import React from 'react';
type Viewport = 'narrow' | 'regular' | 'wide';
export type HiddenProps = {
    on: Array<Viewport> | Viewport;
    children: React.ReactNode;
};
export declare const Hidden: {
    ({ on, children }: HiddenProps): JSX.Element | null;
    displayName: string;
};
export {};
